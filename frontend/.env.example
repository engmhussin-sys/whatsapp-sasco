# Base URL of the backend REST API (must include /api/v1)
NEXT_PUBLIC_API_URL=http://localhost:3000/api/v1

# Base URL of the backend WebSocket server (no path suffix)
NEXT_PUBLIC_WS_URL=http://localhost:3000
